[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

# Forzar regeneración de Prisma Client
[[plugins]]
  package = "@netlify/plugin-nextjs"

